fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
xlrd>=2.0
jinja2>=3.1
openpyxl>=3.1
pandas>=2.0
pyarrow>=14.0
requests>=2.28
structlog>=24.0
python-dotenv>=1.0

# Imported by the centralized SAR pipeline modules that the dashboard
# pulls in via sys.path (sar_detect, sar_aggregate, aisstream_phase1).
rasterio>=1.3
scipy>=1.11
global-land-mask>=1.0
websockets>=12.0

# Scheduler (replaces systemd timers in the container deployment).
apscheduler>=3.10
